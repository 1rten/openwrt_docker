# Ignore Docker's /etc/resolv.conf and force use local dnsmasq
echo "nameserver 127.0.0.1" > /tmp/resolv.conf
mount --bind /tmp/resolv.conf /etc/resolv.conf

# Restart dnsmasq to ensure it picks up any config changes
/etc/init.d/dnsmasq restart

exit 0
